# go-feeds

This project is a RSS news aggregator with concurrent fetching and streamed out put. It fetches all current entries from curated feeds.

## Usage

To run from the source:

```bash
go run feeds.go
```

To build and run:

```bash
# Build
go build -o feeds

# Run
./feeds
```
